set crypt_use_gpgme
# environment
source "~/.mutt/conf.d/env.conf"

# mailboxes
# source "~/.mutt/mailboxes"

# view
source "~/.mutt/conf.d/view.conf"

# colours
source "~/.mutt/conf.d/colours.conf"

# keybindings
source "~/.mutt/conf.d/keys.conf"

# macros
source "~/.mutt/conf.d/macros.conf"

# config not meant for github
source "~/.mutt/conf.d/private.conf"
